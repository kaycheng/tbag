<div class="second-nav d-block m-3">
  <ul class="nav justify-content-center">
    <% categories.each do |category| %>
      <li class="nav-item">
        <%= link_to category.name, category_path(category), class: "nav-link text-decoration-none text-dark font-weight-bold" %>
      </li>
    <% end %>
  </ul>
</div>
